import { SlashCommandBuilder, EmbedBuilder, ChatInputCommandInteraction } from "discord.js";

export const data = new SlashCommandBuilder().setName("اوامر").setDescription("عرض قائمة جميع أوامر البوت");

export async function execute(interaction: ChatInputCommandInteraction) {
  const embed = new EmbedBuilder().setColor(0x5865f2).setTitle("📋 قائمة الأوامر").setDescription("جميع الأوامر الإدارية والعامة المتاحة:")
    .addFields(
      { name: "⚙️ الأوامر العامة", value: ["`/بنج` — فحص استجابة البوت", "`/اوامر` — عرض هذه القائمة", "`/معلومات_عضو` — عرض معلومات عضو", "`/معلومات_سيرفر` — عرض معلومات السيرفر"].join("\n") },
      { name: "🔨 أوامر الحظر والطرد", value: ["`/حظر` — حظر عضو من السيرفر", "`/رفع_حظر` — رفع الحظر عن مستخدم", "`/طرد` — طرد عضو من السيرفر"].join("\n") },
      { name: "🔇 أوامر الكتم", value: ["`/كتم` — كتم عضو مؤقتاً", "`/رفع_كتم` — رفع الكتم عن عضو"].join("\n") },
      { name: "⚠️ أوامر الإنذارات", value: ["`/انذار` — إنذار عضو", "`/الانذارات` — عرض إنذارات عضو", "`/مسح_انذارات` — مسح إنذارات عضو"].join("\n") },
      { name: "💬 أوامر القنوات", value: ["`/مسح` — حذف رسائل من القناة", "`/تباطو` — تفعيل/إيقاف وضع التباطؤ", "`/قفل` — قفل القناة", "`/فتح` — فتح القناة", "`/اعلان` — إرسال إعلان"].join("\n") },
      { name: "🎭 أوامر الرتب والأعضاء", value: ["`/رتبة` — إضافة/إزالة رتبة من عضو", "`/طلب_رتبة` — إعطاء عضو رتبة أو أكثر", "`/ازالة_رتبة` — إزالة رتبة أو أكثر من عضو", "`/تغيير_اسم` — تغيير لقب عضو"].join("\n") },
      { name: "🔒 نظام السجن", value: ["`/سجن` — سجن عضو وإزالة كل رتبه", "`/استرجاع_رتب` — استرجاع رتب العضو بعد السجن"].join("\n") },
      { name: "🚨 نظام المخالفات المرورية", value: ["`/مخالفة` — إصدار مخالفة مرورية", "`/مخالفات` — عرض مخالفات عضو", "`/مخالفاتي` — عرض مخالفاتي", "`/سداد` — تسجيل سداد مخالفة"].join("\n") },
      { name: "🤖 الردود التلقائية والأدوات", value: ["`/ردود_تلقائية اضافة` — إضافة رد تلقائي", "`/ردود_تلقائية حذف` — حذف رد تلقائي", "`/ردود_تلقائية قائمة` — عرض الردود التلقائية", "`/خط` — إرسال خط فاصل", "`/afk` — تفعيل وضع الغياب"].join("\n") }
    ).setFooter({ text: "دارك سيتي | Dark City Bot" }).setTimestamp();
  await interaction.reply({ embeds: [embed], ephemeral: true });
}
